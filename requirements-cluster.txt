torch>=2.1.0
transformers>=4.38.0
tokenizers>=0.15.0
accelerate>=0.27.0
datasets>=2.18.0
einops>=0.7.0
tree-sitter>=0.22.0
tree-sitter-python>=0.21.0
networkx>=3.2.0
scikit-learn>=1.4.0
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.1.0
jsonlines>=4.0.0
pyyaml>=6.0.1
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0
rich>=13.7.0
typer>=0.12.0
libcst<1.0
tabulate>=0.9.0

# E19 — the published J-lens / R-lens (docs/WORKSPACE_LENS.md).
# The estimator is the released reference implementation, vendored in-tree so
# the cluster needs no network and the exact commit is pinned by the checkout:
#     pip install --no-deps -e third_party/jacobian-lens
# --no-deps on purpose: `jlens` declares transformers>=5.5, and this repository
# pins its own transformers version deliberately (see src/models/loader.py's
# tokenizer guard). The package itself is pure Python and adds nothing else.
huggingface_hub>=0.23.0
